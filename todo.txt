#TODO:

level creation for all levels
level growth

ruin_11 is marked as normal john ? (might be fixed with branch to EJBranchRoom)

some branch rooms fail parsing

Fix tried to choose path that has none left (problem with hiding?)
wouldnt be a problem wiht hiding tho bc its supposed to pick a hidden path if no other option exists

